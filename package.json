{
	"name": "shres-world",
	"type": "module",
	"scripts": {
		"fix-sprites": "node scripts/fix-sprite-alpha.mjs",
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"dependencies": {
		"kaplay": "^3001.0.19"
	},
	"devDependencies": {
		"@types/node": "^25.7.0",
		"vite": "^8.0.12"
	}
}
